[phases.install]
cmds = [
  "python -m venv --copies /opt/venv",
  ". /opt/venv/bin/activate && pip install --index-url https://download.pytorch.org/whl/cpu torch==2.3.1+cpu",
  ". /opt/venv/bin/activate && pip install -r requirements.txt"
]